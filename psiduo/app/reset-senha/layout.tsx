import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Redefinir Senha | PsiDuo",
  description: "Recupere o acesso à sua conta.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
